apiVersion: v1
kind: Pod
metadata: 
  name: demo-service-nodeport
 # these labels are used to register this pod with service
  labels: 
    project: roboshop
    component: frontned
    environment: dev
    purpose: nodeport 
spec:
  containers:
  - name: nginx
    image: nginx