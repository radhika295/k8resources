apiVersion: v1
kind: Service
metadata:
  name: demo-service 
spec:
  selector:
    project: roboshop
    component: frontned
    environment: dev 
  ports: 
    - protocol: TCP
      port: 80 # service port
      targetPort: 80 # container port